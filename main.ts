function multiplication(
  number1: number,
  number2: number,
  number3: number
): number {
  return number1 * number2 * number3;
}

function saudacao(nome: string): string {
  return `Olá ${nome}`;
}
